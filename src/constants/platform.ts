/**
 * Platform-wide constants
 */

/**
 * Default platform name used when site.platformName is not set.
 * This allows white-labeling: each site can override this with their own brand.
 */
export const DEFAULT_PLATFORM_NAME = 'FamCircle';

/**
 * Default site name used when no site information is available.
 */
export const DEFAULT_SITE_NAME = 'Family Site';
